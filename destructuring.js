let myDetails = {
    firstName: 'Md.Shamrat',
    lastName: 'Hossain',
    age: 24,
    height: `5' 4"`
}

let { firstName, lastName, age, height } = myDetails

console.log(firstName, lastName, age, height);